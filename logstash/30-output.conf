output {
  if "_grokparsefailure" not in [tags] {
    elasticsearch {
      hosts => ["localhost"]
      manage_template => false
      index => "dish"
    }
  } else {
      elasticsearch {
	hosts => ["localhost"]
	manage_template => false
	index => "parse-failures"
	}
    }
}
